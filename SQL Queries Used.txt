
--To create database -

CREATE DATABASE student_performance;
USE student_performance;


--Create table Student

CREATE TABLE students (
    Student_ID VARCHAR(20),
    Student_Name VARCHAR(100),
    Department VARCHAR(50),
    Semester INT,
    Subject VARCHAR(100),
    Marks INT,
    Attendance FLOAT,
    GPA FLOAT,
    Gender VARCHAR(20)
);

CREATE USER 'powerbi'@'localhost' IDENTIFIED BY 'PowerBI123';

GRANT ALL PRIVILEGES ON student_performance.* TO 'powerbi'@'localhost';

FLUSH PRIVILEGES;



-- Create table Department

CREATE TABLE department (
    department_id INT PRIMARY KEY AUTO_INCREMENT,
    department_name VARCHAR(100) NOT NULL,
    hod_name VARCHAR(100),
    description VARCHAR(255),
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);



INSERT INTO department 
(department_name, hod_name, description)
VALUES
('CSE', 'Dr. Rajesh Sharma', 'Computer Science and Engineering Department'),

('IT', 'Dr. Neha Verma', 'Information Technology Department'),

('ECE', 'Dr. Amit Kulkarni', 'Electronics and Communication Engineering Department'),

('MECH', 'Dr. Vivek Singh', 'Mechanical Engineering Department'),

('CIVIL', 'Dr. Pooja Mehta', 'Civil Engineering Department');




-- Create table Faculty

CREATE TABLE faculty (
    faculty_id INT PRIMARY KEY AUTO_INCREMENT,
    faculty_name VARCHAR(100) NOT NULL,
    email VARCHAR(100) UNIQUE,
    designation VARCHAR(100),
    department_id INT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,

    FOREIGN KEY (department_id)
    REFERENCES department(department_id)
);


INSERT INTO faculty
(faculty_name, email, designation, department_id)
VALUES

('Dr. Ankit Sharma', 'ankit.sharma@college.edu', 'Professor', 1),

('Dr. Priya Verma', 'priya.verma@college.edu', 'Associate Professor', 1),

('Dr. Rahul Mehta', 'rahul.mehta@college.edu', 'Assistant Professor', 2),

('Dr. Sneha Kapoor', 'sneha.kapoor@college.edu', 'Professor', 3),

('Dr. Vivek Rao', 'vivek.rao@college.edu', 'Assistant Professor', 4),

('Dr. Pooja Singh', 'pooja.singh@college.edu', 'Associate Professor', 5);



-- Create table students 

CREATE TABLE student (
    student_id INT PRIMARY KEY AUTO_INCREMENT,
    roll_no VARCHAR(20) UNIQUE,
    student_name VARCHAR(100) NOT NULL,
    gender CHAR(1),
    email VARCHAR(100) UNIQUE,
    semester INT,
    admission_year YEAR,
    department_id INT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,

    FOREIGN KEY (department_id)
    REFERENCES department(department_id)
);


INSERT INTO student
(roll_no, student_name, gender, email, semester, admission_year, department_id)
VALUES

('CSE001', 'Rahul Sharma', 'M', 'rahul.sharma@college.edu', 1, 2024, 1),

('CSE002', 'Priya Singh', 'F', 'priya.singh@college.edu', 2, 2023, 1),

('IT001', 'Aman Verma', 'M', 'aman.verma@college.edu', 3, 2022, 2),

('ECE001', 'Sneha Kapoor', 'F', 'sneha.kapoor@college.edu', 2, 2023, 3),

('MECH001', 'Vivek Rao', 'M', 'vivek.rao@college.edu', 4, 2021, 4),

('CIVIL001', 'Pooja Mehta', 'F', 'pooja.mehta@college.edu', 1, 2024, 5);



-- Create table subject 

CREATE TABLE subject (
    subject_id INT PRIMARY KEY AUTO_INCREMENT,
    subject_code VARCHAR(20) UNIQUE,
    subject_name VARCHAR(100) NOT NULL,
    credits INT,
    semester INT,
    department_id INT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,

    FOREIGN KEY (department_id)
    REFERENCES department(department_id)
);


INSERT INTO subject
(subject_code, subject_name, credits, semester, department_id)
VALUES

('CSE101', 'Python Programming', 4, 1, 1),

('CSE102', 'Data Structures', 4, 2, 1),

('IT201', 'DBMS', 4, 3, 2),

('ECE101', 'Digital Electronics', 3, 2, 3),

('MECH301', 'Thermodynamics', 4, 4, 4),

('CIV101', 'Structural Engineering', 3, 1, 5);


-- Create Table enrollment

CREATE TABLE enrollment (
    enrollment_id INT PRIMARY KEY AUTO_INCREMENT,
    student_id INT,
    subject_id INT,
    enrollment_date DATE,

    FOREIGN KEY (student_id)
    REFERENCES student(student_id),

    FOREIGN KEY (subject_id)
    REFERENCES subject(subject_id)
);

INSERT INTO enrollment
(student_id, subject_id, enrollment_date)
VALUES

(1, 1, '2025-01-10'),

(1, 2, '2025-01-10'),

(2, 1, '2025-01-11'),

(2, 3, '2025-01-11'),

(3, 3, '2025-01-12'),

(4, 4, '2025-01-12'),

(5, 5, '2025-01-13'),

(6, 6, '2025-01-13');


--Create table Attendance

CREATE TABLE attendance (
    attendance_id INT PRIMARY KEY AUTO_INCREMENT,
    student_id INT,
    subject_id INT,
    attendance_percentage DECIMAL(5,2),
    status VARCHAR(20),
    recorded_date DATE,

    FOREIGN KEY (student_id)
    REFERENCES student(student_id),

    FOREIGN KEY (subject_id)
    REFERENCES subject(subject_id)
);

INSERT INTO attendance
(student_id, subject_id, attendance_percentage, status, recorded_date)
VALUES

(1, 1, 92.50, 'Excellent', '2025-02-01'),

(1, 2, 85.00, 'Good', '2025-02-01'),

(2, 1, 74.00, 'Average', '2025-02-02'),

(2, 3, 65.00, 'Average', '2025-02-02'),

(3, 3, 48.00, 'Low', '2025-02-03'),

(4, 4, 88.00, 'Good', '2025-02-03'),

(5, 5, 55.00, 'Average', '2025-02-04'),

(6, 6, 40.00, 'Low', '2025-02-04');


--Create Table marks 

CREATE TABLE marks (
    marks_id INT PRIMARY KEY AUTO_INCREMENT,
    student_id INT,
    subject_id INT,
    internal_marks DECIMAL(5,2),
    external_marks DECIMAL(5,2),
    total_marks DECIMAL(5,2),
    grade VARCHAR(5),

    FOREIGN KEY (student_id)
    REFERENCES student(student_id),

    FOREIGN KEY (subject_id)
    REFERENCES subject(subject_id)
);

INSERT INTO marks
(student_id, subject_id, internal_marks, external_marks, total_marks, grade)
VALUES

(1, 1, 28, 55, 83, 'A'),

(1, 2, 25, 50, 75, 'B+'),

(2, 1, 22, 48, 70, 'B'),

(2, 3, 20, 42, 62, 'C+'),

(3, 3, 15, 30, 45, 'D'),

(4, 4, 26, 54, 80, 'A'),

(5, 5, 18, 40, 58, 'C'),

(6, 6, 12, 25, 37, 'F');


--Create performance report table

CREATE TABLE performance_report (
    report_id INT PRIMARY KEY AUTO_INCREMENT,
    student_id INT,
    semester INT,
    gpa DECIMAL(4,2),
    rank_position INT,
    performance_status VARCHAR(20),
    generated_date DATE,

    FOREIGN KEY (student_id)
    REFERENCES student(student_id)
);

INSERT INTO performance_report
(student_id, semester, gpa, rank_position, performance_status, generated_date)
VALUES

(1, 1, 8.70, 1, 'Excellent', '2025-03-01'),

(2, 2, 7.40, 2, 'Good', '2025-03-01'),

(3, 3, 5.10, 5, 'Average', '2025-03-01'),

(4, 2, 8.20, 3, 'Very Good', '2025-03-01'),

(5, 4, 6.00, 4, 'Average', '2025-03-01'),

(6, 1, 4.20, 6, 'Poor', '2025-03-01');



--Create user table

CREATE TABLE users (
    user_id INT PRIMARY KEY AUTO_INCREMENT,
    username VARCHAR(50) UNIQUE NOT NULL,
    password VARCHAR(100) NOT NULL,
    role VARCHAR(20),
    faculty_id INT NULL,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,

    FOREIGN KEY (faculty_id)
    REFERENCES faculty(faculty_id)
);


INSERT INTO users
(username, password, role, faculty_id)
VALUES

('admin', 'admin123', 'Admin', NULL),

('ankit_faculty', 'faculty123', 'Faculty', 1),

('priya_faculty', 'faculty123', 'Faculty', 2),

('rahul_faculty', 'faculty123', 'Faculty', 3),
('coordinator', 'coord123', 'Coordinator', NULL);



--Create dashboard table

CREATE TABLE dashboard (
    dashboard_id INT PRIMARY KEY AUTO_INCREMENT,
    dashboard_name VARCHAR(100) NOT NULL,
    dashboard_type VARCHAR(50),
    created_by INT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,

    FOREIGN KEY (created_by)
    REFERENCES users(user_id)
);


INSERT INTO dashboard
(dashboard_name, dashboard_type, created_by)
VALUES

('Admin Dashboard', 'Admin', 1),

('Faculty Dashboard - CSE', 'Faculty', 2),

('Faculty Dashboard - IT', 'Faculty', 3),

('Student Performance Analytics', 'Analytics', 1),

('At-Risk Student Dashboard', 'Analytics', 1);



-- At Risk

SELECT 
    s.student_name,
    d.department_name,
    pr.gpa,

    ROUND(AVG(a.attendance_percentage), 2) 
        AS avg_attendance,

    CASE
        WHEN pr.gpa < 5 
             OR AVG(a.attendance_percentage) < 50
        THEN 'High Risk'

        WHEN pr.gpa < 7
             OR AVG(a.attendance_percentage) < 70
        THEN 'Medium Risk'

        ELSE 'Low Risk'
    END AS risk_level

FROM student s

JOIN department d
    ON s.department_id = d.department_id

JOIN performance_report pr
    ON s.student_id = pr.student_id

JOIN attendance a
    ON s.student_id = a.student_id

GROUP BY
    s.student_name,
    d.department_name,
    pr.gpa;SELECT 
    s.student_name,
    d.department_name,
    pr.gpa,

    ROUND(AVG(a.attendance_percentage), 2) 
        AS avg_attendance,

    CASE
        WHEN pr.gpa < 5 
             OR AVG(a.attendance_percentage) < 50
        THEN 'High Risk'

        WHEN pr.gpa < 7
             OR AVG(a.attendance_percentage) < 70
        THEN 'Medium Risk'

        ELSE 'Low Risk'
    END AS risk_level

FROM student s

JOIN department d
    ON s.department_id = d.department_id

JOIN performance_report pr
    ON s.student_id = pr.student_id

JOIN attendance a
    ON s.student_id = a.student_id

GROUP BY
    s.student_name,
    d.department_name,
    pr.gpa;
	
	
	
	
-- Create View 

CREATE VIEW student_performance_view AS

SELECT 
    s.student_id,
    s.student_name,
    d.department_name,
    pr.semester,
    pr.gpa,
    ROUND(AVG(a.attendance_percentage), 2) 
        AS avg_attendance,

    CASE
        WHEN pr.gpa < 5 
             OR AVG(a.attendance_percentage) < 50
        THEN 'High Risk'

        WHEN pr.gpa < 7
             OR AVG(a.attendance_percentage) < 70
        THEN 'Medium Risk'

        ELSE 'Low Risk'
    END AS risk_level

FROM student s

JOIN department d
    ON s.department_id = d.department_id

JOIN performance_report pr
    ON s.student_id = pr.student_id

JOIN attendance a
    ON s.student_id = a.student_id

GROUP BY
    s.student_id,
    s.student_name,
    d.department_name,
    pr.semester,
    pr.gpa;